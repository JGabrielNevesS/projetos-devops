#SPDX-License-Identifier: MIT-0
---
# handlers file for nginx

- name: recarregar nginx
  ansible.builtin.service:
    name: nginx
    state: reloaded

- name: Salvar regras do firewall
  ansible.builtin.command:
    cmd: /usr/sbin/netfilter-persistent save
  changed_when: true